export const HybridFrameConverter = {
  convertFrameToImage: jest.fn(),
};

export const useCameraDevice = jest.fn();
export const useFrameOutput = jest.fn();
export const Camera = (props: any) => null;
export type Frame = any;
